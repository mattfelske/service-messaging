module.exports = (locals) => {

  console.log('Performing application startup ...');
  // TODOs
  // Register with Service Registry
  // Retrieve dependent services
  // Setup heartbeat with service registry based on interval
};
